---
import Layout from "components/layout.astro"

const { user } = Astro.locals
if (user === undefined) return Astro.redirect('/auth')
---
<Layout>
    <h1>Dashboard</h1>
    <p>Welcome, {user.displayName}!</p>
    {user.data.notes.map(note => <p>{note}</p>)}
</Layout>
